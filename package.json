{
  "name": "layout-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Source-first workbench for pluggable visual editors.",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "engines": {
    "node": ">=22.23.0"
  },
  "scripts": {
    "dev": "vite examples/pug-sankey --host 0.0.0.0",
    "build:packages": "npm run build --workspace @briday1/layout && npm run build --workspace @briday1/layout-pug-sankey",
    "build": "npm run build:packages && vite build examples/pug-sankey --base=./",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "preview": "vite preview examples/pug-sankey --host 0.0.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/briday1/_layout"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "jsdom": "30.0.1",
    "typescript": "5.9.3",
    "vite": "8.2.2",
    "vitest": "5.0.0"
  }
}
